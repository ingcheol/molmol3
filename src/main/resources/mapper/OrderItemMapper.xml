<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.sm.molshop3.repository.OrderItemRepository">

    <!-- ✅ 주문 아이템 등록 -->
    <insert id="insert" parameterType="edu.sm.molshop3.dto.OrderItem">
        INSERT INTO orderitem (order_id, product_id, quantity, price)
        VALUES (#{orderId}, #{productId}, #{quantity}, #{price})
    </insert>

    <!-- ✅ 주문 아이템 삭제 -->
    <delete id="delete">
        DELETE FROM orderitem
        WHERE order_id = #{orderId} AND product_id = #{productId}
    </delete>

    <!-- ✅ resultMap 정의 (product_name 포함) -->
    <resultMap id="OrderItemResultMap" type="edu.sm.molshop3.dto.OrderItem">
        <result property="orderId" column="order_id"/>
        <result property="productId" column="product_id"/>
        <result property="quantity" column="quantity"/>
        <result property="price" column="price"/>
        <result property="productName" column="product_name"/>
    </resultMap>

    <!-- ✅ 주문 ID로 주문 아이템 조회 (product_name JOIN 포함) -->
    <select id="selectByOrderId" parameterType="int" resultMap="OrderItemResultMap">
        SELECT oi.*, p.product_name
        FROM orderitem oi
                 JOIN product p ON oi.product_id = p.product_id
        WHERE oi.order_id = #{orderId}
    </select>

</mapper>
